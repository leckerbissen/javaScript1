<meta charset="utf-8">
<script>

    /*
    1. Складываем числа 10 + 10, получаем 20
    2. Число 20 переводим в строку, получаем "20"
    3. Складываем строки "20" и "10", получаем "2010"
    4. "2010" присваем переменной result
    5. Выводим переменную result в консоли через console.log()
    */
    let result = 10 + 10 + "10";
    console.log(result);

    /*
    1. Переводим первое значение 10 из числа в строку, получаем "10"
    2. Складываем первые два значения (строки) "10" и "10", получаем "1010"
    3. Последнее значение 10 из числа переводим в строку, получаем "10"
    4. Складываем "1010" и "10", получаем "101010"
    5. "101010" присваем переменной result
    6. Выводим переменную result в консоли через console.log()
    */
    result = 10 + "10" + 10;
    console.log(result);

    /*
    1. Складываем первые два значения (числа) 10 и 10, получаем 20
    2. Переводим последние значение "10" из строки в число при помощи унарного плюса, получаем 10
    3. 20 + 10, получаем 30
    4. 30 присваем переменной result
    5. Выводим переменную result в консоли через console.log()
    */
    result = 10 + 10 + +"10";
    console.log(result);

    /*
    1. Пустое значение строки "" переводим в число 0
    2. Присваиваем 0 отрицательное значение, получаем -0
    3. 10 / -0, получаем -Infinity - минус бесконечность
    4. -Infinity присваем переменной result
    5. Выводим переменную result в консоли через console.log()
    */
    result = 10 / -"";
    console.log(result);

    /*
    1. "2,5" не удастся перевести в число, т.к. в языке принято использовать точку . для десячичных чисел.
    2. Математическая операция / + не может быть выполнена корректно
    3. Переменной result присвается значение NaN - not a number
    4. Выводим переменную result в консоли через console.log()
    */
    result = 10 / +"2,5";
    console.log(result);
</script>